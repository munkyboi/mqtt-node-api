# Mock API Responses

This folder contains example JSON responses for the current API endpoints.

## Files

- `health.json` for `GET /health`
- `status.json` for `GET /status`
- `chart.json` for `GET /chart`
- `relay-on.json` for `POST /relay/ON`
- `relay-off.json` for `POST /relay/OFF`
